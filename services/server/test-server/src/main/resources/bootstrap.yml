spring:
  main:
    #懒加载
    lazy-initialization: true
  profiles:
    active: dev
  application:
    name: test-app
server:
  servlet:
    context-path: /test
    encoding:
      charset: UTF-8
      force: true
  tomcat:
    uri-encoding: UTF-8

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml

