


spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 19940825Ls
    url: jdbc:mysql://120.48.170.201:3306/test?serverTimezone=UTC&useUnicode=true&characterEncoding=utf8
server:
  port: 9025
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl #开启sql日志
    # 该配置就是将带有下划线的表字段映射为驼峰格式的实体类属性
    map-underscore-to-camel-case: true
  mapper-locations: classpath*:/mapper/**/*.xml