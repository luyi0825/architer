<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <artifactId>server-parent</artifactId>
        <groupId>com.bussiness</groupId>
        <version>1.0.0</version>
    </parent>
    <artifactId>search-server</artifactId>
    <name>search-server</name>
    <packaging>jar</packaging>
    <dependencies>
        <dependency>
            <groupId>com.architecture.ultimate</groupId>
            <artifactId>boot-starter-web-mvc</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.architecture.ultimate</groupId>
            <artifactId>elastic-search-dml</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.architecture.ultimate</groupId>
            <artifactId>elastic-search-model</artifactId>
            <version>1.0.0</version>
        </dependency>
        <dependency>
            <groupId>com.architecture.ultimate</groupId>
            <artifactId>elastic-search-mq-consumer</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>com.architecture.ultimate</groupId>
            <artifactId>test</artifactId>
            <version>${project.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <image>
                        <!--                        执行完build 不自动pushfalse -->-->
                        <name>test/${project.artifactId}</name>
                    </image>
                    <docker>
                        <!--远程docker daemon的连接地址和端口-->
                        <host>tcp://192.168.2.222:2375</host>
                        <!--如果使用https协议需要设置为true-->
                        <tlsVerify>false</tlsVerify>
                        <publishRegistry>
                            <username>893431435@qq.com</username>
                            <password>19940825Ls.</password>
                            <url>registry.cn-hangzhou.aliyuncs.com/luyi_public/architecture.ultimate</url>
                        </publishRegistry>
                    </docker>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
