server:
  port: 8880
  servlet:
    context-path: /
    encoding:
      charset: utf-8
      enabled: true
      force: true
  tomcat:
    uri-encoding: UTF-8

spring:
  profiles:
    active: test

rocketmq:
  name-server: 127.0.0.1:9876
  consumer:
    group: test
  producer:
    group: test
