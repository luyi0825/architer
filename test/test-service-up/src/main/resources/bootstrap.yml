
##应用名称配置
spring:
  main:
    allow-bean-definition-overriding: true
  application:
    ##会员服务
    name: test-service
## nacos配置
  cloud:
    nacos:
      config:
        server-addr: 120.48.170.201:8848
        ##配置文件拓展名
        file-extension: yml
        ## 命名空间
        namespace: public
      ## 注册中心配置
      discovery:
        server-addr: 120.48.170.201:8848
        #group: dev
        namespace: public
        group: DEFAULT_GROUP
        enabled: true
        metadata:
          version: 1
    architer:
      load-balance:
        strategy: round
        grayscale-release: true







