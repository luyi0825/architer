spring:
  profiles:
    active: out

server:
  port: 9017
  tomcat:
    threads:
      max: 50

##rabbitmq相关配置,暴露bus刷新配置的端点
management:
  endpoints: #暴露bus刷新配置的端点
    web:
      exposure:
        include: 'bus-refresh'



