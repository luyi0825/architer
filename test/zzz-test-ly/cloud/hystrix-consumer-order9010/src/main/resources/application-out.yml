spring:
  cloud:
    zookeeper:
      connect-string: 124.71.110.204:2181

logging:
  level:
    # 接口全路径：debug级别日志
    com.lz.core.test.hystrix.order.feign: debug
feign:
  client:
    config:
      # 想要调用的微服务名称
      hystrix-provider-zk-payment:
        loggerLevel: full